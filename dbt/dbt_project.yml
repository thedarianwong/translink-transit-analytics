name: translink_analytics
version: '1.0.0'
config-version: 2

profile: translink_analytics

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  translink_analytics:
    staging:
      +schema: STAGING
      +materialized: view
    marts:
      +schema: ANALYTICS
      +materialized: table

seeds:
  translink_analytics:
    +schema: ANALYTICS
